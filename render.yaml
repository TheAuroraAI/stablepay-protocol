services:
  - type: web
    name: stablepay-api
    env: node
    plan: free
    buildCommand: npm install --include=dev
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: CLUSTER
        value: devnet
      - key: X402_ENABLED
        value: "false"
